import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff100B21);
const kTransiationDuration = Duration(microseconds: 250);//because if the clint tell me that the transaion between screens is slow or fast i can exchange it from here easily